import { Button } from '@mui/material';

function Start(): JSX.Element {
  return (
    <Button color="primary" variant="contained" href="/doglist">
      Get a list of dogs!
    </Button>
  );
}

export default Start;
